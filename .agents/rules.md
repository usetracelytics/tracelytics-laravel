# Project Rules

- Do NOT use the `DSN` keyword anywhere in configuration, documentation, comments, or code. Use alternative naming such as `API_KEY`, `PROJECT_KEY`, `INGESTION_KEY`, or `ENDPOINT` instead.
